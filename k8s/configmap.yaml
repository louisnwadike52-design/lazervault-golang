apiVersion: v1
kind: ConfigMap
metadata:
  name: lazervault-config
  namespace: default # Or your specific namespace
data:
  GRPC_SERVER_PORT: "7007"
  HTTP_SERVER_PORT: "8080"
  # Add other non-sensitive configurations here
  # Example: ENV: "production"
  REDIS_SERVER_ADDR: "redis-service:6379"