steps:
  # Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      [
        'build',
        '-f',
        'Dockerfile.http2',
        '-t',
        'europe-west1-docker.pkg.dev/lazervault/lazervault/lazervault-golang-http2:latest',
        '.',
      ]
  # Push the container image to Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'europe-west1-docker.pkg.dev/lazervault/lazervault/lazervault-golang-http2:latest']
  # Deploy container image to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      [
        'run',
        'deploy',
        'lazervault-golang-http2',
        '--image',
        'europe-west1-docker.pkg.dev/lazervault/lazervault/lazervault-golang-http2:latest',
        '--region',
        'europe-west1',
        '--platform',
        'managed',
        '--port',
        '7007',
        '--use-http2',
        '--quiet',
        '--allow-unauthenticated',
      ]
images:
  - 'europe-west1-docker.pkg.dev/lazervault/lazervault/lazervault-golang-http2:latest'
